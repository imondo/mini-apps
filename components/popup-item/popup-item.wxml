<view class="drawer_screen" bindtap="hideModal" wx:if="{{showModalStatus}}" hidden='{{isHidden}}'></view>
<view animation="{{animationData}}" class="drawer_attr_box" wx:if="{{showModalStatus}}">
  <view class="drawer_attr_content" style='height: {{visibleHeight + "px"}}'>
    <view class="text_center">
    </view>
    <view class="drawer_desc">
      <slot></slot>
    </view>
  </view>
  <view class="btn open_btn" bindtap="hideModal" data-status="1">关闭</view>
</view>