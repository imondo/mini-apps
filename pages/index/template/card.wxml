<template name="card">
  <view class="card">
    <block wx:for="{{card}}" wx:for-index="index" wx:key="index">
      <view class="card-item">
        <view class="card-text">{{cardValue[index]}}</view>
        <view class="card-uom">{{item}}</view>
      </view>
    </block>
  </view>
</template>